"""Policy agent — hybrid RAG over policy documents with cited clauses."""

from __future__ import annotations

from claimguard.schemas.graph import GraphState


def run_policy_agent(state: GraphState) -> GraphState:
    raise NotImplementedError("policy_agent is implemented in Phase 3")
